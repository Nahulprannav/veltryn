# Example env file for local development
# Copy this file to .env.local and fill values if you want Firebase or Socket server enabled

# Optional Firebase configuration (if you want real OAuth via Firebase Auth)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Optional Socket.IO server URL (used for self-hosted realtime)
# Example: http://localhost:4000
VITE_SOCKET_URL=http://localhost:4000

# Notes:
# - After adding .env.local, restart the dev server (npm run dev).
# - Firebase is optional; when configured, Auth flows will use Firebase.